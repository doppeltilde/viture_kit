name: viture_kit
description: "Native Dart FFI bindings for the VITURE XR Glasses SDK."
version: 0.0.5
homepage: "https://www.viture.com/"
repository: "https://github.com/doppeltilde/viture_kit"

environment:
  sdk: ^3.13.2

platforms:
  macos:

topics:
  - viture
  - xr
  - macos
  - ffi

dependencies:
  code_assets: ^1.1.0
  hooks: ^2.0.0
  logging: ^1.3.0
  native_toolchain_c: ^0.19.0
  # dart_libusb: ^1.0.1
  hidapi:
    git: https://github.com/doppeltilde/hidapi
  ffi: ^2.1.4

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0